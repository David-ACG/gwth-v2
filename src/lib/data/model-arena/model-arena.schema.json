{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gwth.ai/schemas/model-arena-lab.schema.json",
  "title": "Model Arena lab",
  "description": "A GWTH lab: one realistic task, two AI models run head-to-head on the same prompt, outputs shown verbatim side by side, a beginner rubric, and a dated verdict. Deliberately perishable; superseded when models change. See MODEL_ARENA_TEMPLATE.md.",
  "type": "object",
  "required": [
    "id",
    "slug",
    "format",
    "title",
    "status",
    "brief",
    "matchup",
    "prompt",
    "outputs",
    "rubric",
    "verdict",
    "tryItYourself",
    "testedOn"
  ],
  "additionalProperties": false,
  "properties": {
    "id": { "type": "string", "description": "Stable unique id, e.g. arena_001." },
    "slug": { "type": "string", "description": "URL-friendly slug." },
    "format": { "const": "model-arena", "description": "Discriminates this from the retired tiered-lab format." },
    "title": { "type": "string", "description": "Plain and specific: the task, then the matchup." },
    "status": {
      "type": "string",
      "enum": ["live", "archived"],
      "description": "live = one of the ~6 current labs; archived = superseded but kept for the archive."
    },
    "category": { "type": "string", "description": "Broad task area, e.g. Writing, HR, Research, Admin." },
    "testedOn": { "type": "string", "format": "date", "description": "The date both outputs were generated (ISO yyyy-mm-dd)." },
    "brief": {
      "type": "string",
      "description": "One paragraph: who the student pretends to be, the realistic task, why it matters. Plain English."
    },
    "summary": {
      "type": "string",
      "description": "The brief in a sentence or two, for the opening of the lab page and the read-aloud control. Never replaces brief."
    },
    "preview": {
      "type": "object",
      "description": "The glanceable preview shown on the labs index: a plain task cue, an outcome line, and a specimen composed from the lab's OWN material. Never an illustration, never an invented screenshot.",
      "required": ["taskCue", "outcome", "specimen"],
      "additionalProperties": false,
      "properties": {
        "taskCue": { "type": "string", "description": "Plain task cue in a visitor's own words, e.g. Spreadsheet clean-up." },
        "outcome": { "type": "string", "description": "One outcome-led line: what the visitor walks away knowing." },
        "specimen": {
          "description": "A few lines of the lab's real material, drawn in CSS on the card.",
          "oneOf": [
            {
              "type": "object",
              "required": ["kind", "caption", "columns", "rows"],
              "additionalProperties": false,
              "properties": {
                "kind": { "const": "grid" },
                "caption": { "type": "string", "description": "Where the rows come from." },
                "columns": { "type": "array", "minItems": 2, "maxItems": 4, "items": { "type": "string" } },
                "rows": {
                  "type": "array",
                  "minItems": 2,
                  "maxItems": 4,
                  "items": {
                    "type": "object",
                    "required": ["cells"],
                    "additionalProperties": false,
                    "properties": {
                      "cells": { "type": "array", "minItems": 2, "maxItems": 4, "items": { "type": "string" } },
                      "note": { "type": "string", "description": "Why this row is interesting, in a few words. Omit for a clean row." }
                    }
                  }
                }
              }
            },
            {
              "type": "object",
              "required": ["kind", "caption", "items"],
              "additionalProperties": false,
              "properties": {
                "kind": { "const": "pairs" },
                "caption": { "type": "string", "description": "Where the pairs come from." },
                "items": {
                  "type": "array",
                  "minItems": 2,
                  "maxItems": 3,
                  "items": {
                    "type": "object",
                    "required": ["label", "value"],
                    "additionalProperties": false,
                    "properties": {
                      "label": { "type": "string" },
                      "value": { "type": "string" }
                    }
                  }
                }
              }
            }
          ]
        }
      }
    },
    "video": {
      "type": "object",
      "description": "A REAL, playable video guide. Present ONLY when the media exists and plays. Absence is the honest default and is what makes the planned state truthful; never add this to imply a video that has not been made.",
      "required": ["src"],
      "additionalProperties": false,
      "properties": {
        "src": { "type": "string", "description": "Playable source URL. Must resolve to real media." },
        "poster": { "type": "string", "description": "Poster frame shown before play." },
        "captions": { "type": "string", "description": "WebVTT captions track." },
        "durationLabel": { "type": "string", "description": "Human running time as authored, e.g. 4 min. Never estimated." }
      }
    },
    "matchup": {
      "type": "array",
      "description": "Exactly two contestants.",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "required": ["name", "modelId", "howRun"],
        "additionalProperties": false,
        "properties": {
          "name": { "type": "string", "description": "Friendly product name, e.g. Claude, ChatGPT." },
          "modelId": { "type": "string", "description": "Exact model id, never rounded, e.g. claude-opus-4-8, gpt-5.6-sol." },
          "modelLabel": { "type": "string", "description": "Human model label, e.g. Claude Opus 4.8." },
          "howRun": { "type": "string", "description": "Exactly how it was run, e.g. Codex CLI 0.144.1 at high reasoning effort." }
        }
      }
    },
    "prompt": {
      "type": "string",
      "description": "The single prompt given to BOTH models, verbatim and identical."
    },
    "outputs": {
      "type": "array",
      "description": "Each contestant's real answer, quoted verbatim. Same order as matchup.",
      "minItems": 2,
      "maxItems": 2,
      "items": {
        "type": "object",
        "required": ["by", "verbatim"],
        "additionalProperties": false,
        "properties": {
          "by": { "type": "string", "description": "Which contestant produced it (matches matchup.name)." },
          "verbatim": { "type": "string", "description": "The output exactly as generated. Never edited, trimmed or paraphrased." }
        }
      }
    },
    "rubric": {
      "type": "array",
      "description": "4-6 criteria a beginner can score by reading.",
      "minItems": 4,
      "maxItems": 6,
      "items": {
        "type": "object",
        "required": ["criterion", "goodLooksLike"],
        "additionalProperties": false,
        "properties": {
          "criterion": { "type": "string", "description": "A plain question the student answers for each output." },
          "goodLooksLike": { "type": "string", "description": "One line on what a strong answer looks like." }
        }
      }
    },
    "verdict": {
      "type": "object",
      "required": ["callText", "freshnessNote"],
      "additionalProperties": false,
      "properties": {
        "winner": { "type": "string", "description": "Contestant name, or 'Close' / 'Tie'. Honest, never rigged." },
        "callText": { "type": "string", "description": "The reasoned verdict, fair to the loser." },
        "freshnessNote": { "type": "string", "description": "Reminder that models change, so this holds only for these versions and this date." }
      }
    },
    "tryItYourself": {
      "type": "array",
      "description": "Short numbered steps for the student to run the same task themselves.",
      "minItems": 1,
      "items": { "type": "string" }
    }
  }
}
